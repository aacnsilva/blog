<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="css/styles.css" />
    <title>
      {{#if post_title}}
        {{post_title}} - António's DevLog
      {{else}}
        António's DevLog
      {{/if}}
    </title>
  </head>
  <body>
    {{{header}}}
    {{{content}}}
  </body>
</html>
